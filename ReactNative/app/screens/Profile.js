import React from 'react'
import { Text, View, Button } from 'react-native'

export default function Profile() {
    return (
        <View>
            <Text>profile page</Text>
            <Text>I don't expect it to need any added navigation from it. will need logout moved to here once base navigation is fixed.</Text>
        </View>
    )
} 